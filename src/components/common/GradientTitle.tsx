const GradientTitle = ({ title }: { title: string }) => {
  return (
    <div className="text-yellow-400 text-base font-bold font-['Pretendard Variable']">
      {title}
    </div>
  )
}

export default GradientTitle
